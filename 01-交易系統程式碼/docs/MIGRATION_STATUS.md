# 🔄 Streamlit → HTML 遷移狀態報告

## 📋 功能對應表

### ✅ 已遷移到 HTML 的功能

| Streamlit 頁面 | 功能 | HTML 對應 | 進度 |
|----------------|------|----------|------|
| **0_🏠_home.py** | 首頁儀表板 | Dashboard | ✅ |
| | 投資組合概覽 | /api/holdings | ✅ |
| | NAV 圖表 | 績效圖表 | ✅ |
| **1_💹_實盤交易管理系統.py** | 交易列表展示 | 💹 實盤交易頁面 | ✅ |
| | 券商 Positions 同步 | /api/sync-broker-positions | ✅ |
| | 風險監控（基礎） | 系統狀態指示 | ⚠️ 部分 |
| **2_📁_策略上傳與績效.py** | 策略上傳表單 | ❌ **缺失** | ❌ |
| | 績效回測圖表 | 📈 多策略對比頁面 | ⚠️ 基礎版 |
| | Sharpe Ratio 計算 | ⚠️ 後端支持但UI缺 | ⚠️ |
| **3_📊_多策略績效比較.py** | 多策略並排對比 | 📈 多策略對比 | ✅ |
| | 績效指標表格 | /api/metrics | ✅ |
| **4_📈_全能策略管理與比較.py** | 進階策略分析 | ⚙️ 進階分析 | ⚠️ 基礎版 |

---

## ❌ 完全缺失的功能（需要補完）

### 1️⃣ **策略上傳與管理** (優先級：高)
```
Streamlit: pages/2_📁_策略上傳與績效.py 中的功能
├─ CSV 策略文件上傳表單
├─ 自動計算 Sharpe Ratio、勝率、最大回撤
├─ 將績效數據寫入 daily_nav 表
└─ 視覺化回測曲線

HTML 缺失:
└─ 沒有上傳表單頁面
└─ 沒有績效計算邏輯
```

### 2️⃣ **風控日誌與風險警報** (優先級：中)
```
Streamlit: 1_實盤交易管理系統.py 中的功能
├─ 風控攔截日誌展示
├─ 實時風險評分
├─ 持倉警告提示
└─ 風險配置參數

HTML 缺失:
└─ 沒有 risk_incidents 表的展示
└─ 沒有風控面板
```

### 3️⃣ **交易原因編輯** (優先級：中)
```
Streamlit: 1_實盤交易管理系統.py 中的功能
├─ 直接編輯「進場原因」、「出場原因」
├─ 批量更新交易備註
└─ 實時寫回 Google Sheets

HTML 缺失:
└─ 沒有編輯表單
└─ 表格為只讀
```

### 4️⃣ **Intel Events（情報層）** (優先級：低)
```
Streamlit: 目前沒有專門頁面
HTML:
└─ 可能在進階分析中

實現功能:
├─ USGS 地震監測
├─ 新聞風險事件
├─ 波斯灣油輪監測 (✅ 已實現)
└─ 總經風險評分
```

---

## 📊 遷移進度統計

```
總功能數：15+
✅ 已遷移：7-8 個 (47%)
⚠️  部分遷移：4 個 (27%)
❌ 未遷移：3-4 個 (26%)
```

---

## 🎯 補完建議（優先順序）

### 🔴 Phase 1（本周完成）- 核心功能
1. **策略上傳表單**
   - 創建 HTML 頁面 + 文件上傳表單
   - 後端計算回測績效
   - 寫入 strategies 和 daily_nav 表

2. **交易編輯功能**
   - 將交易列表改為可編輯的表格
   - 支持直接修改「進場原因」、「出場原因」
   - POST /api/trades/update 端點

### 🟠 Phase 2（次周完成）- 風控面板
3. **風控日誌展示**
   - 創建 risk_incidents 表的展示頁面
   - 實時警報提示
   - 風險配置管理

### 🟡 Phase 3（按需）- 進階分析
4. **Intel Events 整合**
   - 在進階分析頁面顯示地震、新聞、油輪
   - 風險評分聚合

---

## 🔧 技術方案

### 缺失的 API 端點（需要創建）

```python
# 策略相關
POST /api/strategies/upload          # 上傳策略 CSV
GET  /api/strategies/performance     # 獲取策略績效
POST /api/strategies/calculate       # 計算回測指標

# 交易編輯
POST /api/trades/update              # 更新交易記錄
POST /api/trades/bulk-update         # 批量更新

# 風控相關
GET  /api/risk-incidents             # 獲取風控日誌
GET  /api/risk-summary               # 風控摘要
POST /api/risk-config                # 更新風控參數

# Intel
GET  /api/intel-events               # 獲取情報事件
GET  /api/macro-state                # 總經狀態
```

---

## 📝 下一步行動

### 我需要你確認：
1. ✏️ **優先遷移哪個功能？** (推薦：策略上傳)
2. 📅 **時間限制？**
3. 🎨 **HTML 設計風格？** (是否保持現有主題)

### 我可以幫助：
- [ ] 創建策略上傳頁面 + 後端邏輯
- [ ] 實現交易編輯功能
- [ ] 構建風控儀表板
- [ ] 集成 Intel Events 層

---

## 💾 相關文件位置

| 功能 | Streamlit 文件 | HTML 文件 | API 文件 |
|------|----------------|----------|---------|
| 策略 | pages/2_📁_*.py | ❌ 無 | ❌ 缺失 |
| 交易 | pages/1_💹_*.py | templates/dashboard.html | app_simple.py |
| 風控 | pages/1_💹_*.py | ❌ 無 | ❌ 缺失 |
| Intel | ❌ 無 | ⚠️ 部分 | ✅ 後端有 |

